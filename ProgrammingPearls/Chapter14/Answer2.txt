为每个块i添加一个指针，该指针指向块2i
第一个块序号为0，类似于快速幂，可以设计如下算法
    1. 假设要找的是块k，将k表示为二进制串的形式
    2. 从块0出发，同时从高位到低位遍历该二进制串
        i. 当前位为1 指针移向当前块所指的下一块
        ii. 否则指针不动
        iii. 指针移向当前块的倍数块，然后遍历二进制串的下一位
    3. 最终指针指向的块就是所求块