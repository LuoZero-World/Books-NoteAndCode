在m < n/2时，假设这一次选中在集合中的元素的概率为p;选中不在集合中的概率为(1-p)
X为当前m下进行成员测试的次数
E[X] = (1-p)*1 + p*(1+E[x])
E[X] = 1/(1-p)
而1-p > 1/2，故此时进行成员测试的期望小于2

当m=n时，程序平均要做nlnn次成员测试(原理同“赠券收集问题”)
每个数都要进行至少1次成员测试，若成员测试次数大于m，则说明此时有两个数重复了。一般来说当n>>m时，m=O(√n) 时成员测试次数会大于m(原理同“生日悖论”)